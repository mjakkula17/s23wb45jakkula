extends layout

block content
  h1= title
  p Welcome to #{title}
  P Mounika Jakkula Express Lab10 Express #{title}
  button(type="button", class="btn btn-secondary", onclick="doReduce()") Press Me

  div(id = "reduce") Reduce the value
  a(href="earphones") LINK
  script.
      count = 20
      change  = -1
      function doReduce(){
          count += change;
          change -= 1;
          document.getElementById('reduce').innerHTML = `Count ${count} Change ${change}`;
      }